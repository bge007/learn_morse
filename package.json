{
  "name": "morse-code-studio",
  "version": "1.4.0",
  "private": true,
  "description": "Morse Code Studio — Android app (Capacitor WebView wrapper around index.html)",
  "scripts": {
    "build:web": "node copy-web.mjs",
    "sync": "node copy-web.mjs && cap sync android",
    "apk": "node copy-web.mjs && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/text-to-speech": "^5.1.0",
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
